syntax = "proto3";

package me.sticnarf.agga.messages;

message Connect {
    string key = 1;
    repeated string knownServers = 2;
}

message ServerInfo {
    string id = 1;
    string address = 2;
}

message ServerList {
    repeated ServerInfo servers = 1;
}